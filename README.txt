To run this code, ensure that you are in the same directory as unsupervised.py and the data directory. Then simply run using
python unsupervised.py

All plots will be saved in the same directory. Note that it may take a while to run as it generates all the plots.